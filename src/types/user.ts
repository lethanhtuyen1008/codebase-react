export namespace User {
  export type UserInfo = {
    id: number;
    firstName: string;
    lastName: string;
    dateOfBirth: string;
    email: string;
    platform: string;
    role: number;
    status: number;
  };
}
